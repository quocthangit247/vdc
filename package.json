{
  "name": "icommerce-server",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": {
    "packages": [
      "microservices/*"
    ]
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "test:watch": "lerna run test:watch --parallel"
  }
}
